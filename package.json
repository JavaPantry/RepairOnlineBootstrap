{
  "name": "RepairOnline",
  "description": "Repair Online",
  "private": true,
  "scripts": {
    "clean": "rimraf dist",
    "prebuild": "npm run clean",
    "build": "webpack --progress --profile --colors --display-error-details --display-cached",
    "buildProd": "webpack --config webpack.prod.config.js --progress --profile --colors --display-error-details --display-cached",
    "startWebpackDevServer": "webpack-dev-server --inline --colors --progress --display-error-details --disaply-modules --display-cached --port 8080"
  },
  "dependencies": {
    "bootstrap": "^3.3.7",
    "bootstrap-datepicker": "^1.6.4",
    "bootstrap-validator": "^0.11.8",
    "jquery": "^2.2.2",
    "jquery-validation": "^1.16.0"
  }

}
